import { ShipmentTimeline } from '@/components/fulfillment/ShipmentTimeline';
export default function FulfillmentPage() { return <div className="container-page py-10"><h1 className="text-4xl font-black">Fulfillment operations</h1><p className="mt-2 text-slate-600">Warehouse inventory, seller-fulfilled orders, split shipments and carrier exceptions.</p><div className="mt-8 grid gap-6 lg:grid-cols-2"><ShipmentTimeline /><div className="rounded-3xl bg-white p-6 shadow-card"><h2 className="text-2xl font-black">Shipment controls</h2>{['Create shipment', 'Print packing slip', 'Add tracking number', 'Mark shipped', 'Handle lost package', 'Record delivery photo'].map(x => <button key={x} className="mr-3 mt-3 rounded-full border px-4 py-2 font-bold">{x}</button>)}</div></div></div>; }
