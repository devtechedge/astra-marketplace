import { DashboardCard } from '@/components/DashboardCard';
import { AdminNav } from '@/components/admin/AdminNav';
import { orders, products, sellers, tickets } from '@/lib/demoData';
import { formatMoney } from '@/lib/commerce';

export default function AdminPage() {
  return <div className="container-page py-10"><AdminNav /><div><p className="font-bold text-brand">Admin operations portal</p><h1 className="text-4xl font-black">Marketplace command center</h1><p className="mt-1 text-slate-600">admin@demo.com / Demo123!</p></div><div className="mt-8 grid gap-5 md:grid-cols-4"><DashboardCard title="GMV" value="$128.4K" note="+18% month over month" /><DashboardCard title="Orders" value={String(orders.length + 4910)} note="Lifecycle monitored" /><DashboardCard title="Refund rate" value="2.4%" note="Finance queue healthy" /><DashboardCard title="Support SLA" value="96%" note="Tickets within target" /></div><div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="rounded-3xl bg-white p-6 shadow-card"><h2 className="text-2xl font-black">Seller moderation</h2><div className="mt-4 space-y-3">{sellers.map(s => <div key={s.id} className="flex items-center justify-between rounded-2xl bg-slate-50 p-4"><div><p className="font-bold">{s.name}</p><p className="text-sm text-slate-500">Rating {s.rating} · Payout {formatMoney(s.payoutBalance)}</p></div><span className="rounded-full bg-indigo-50 px-3 py-1 text-sm font-bold text-indigo-700">{s.status}</span></div>)}</div></section><section className="rounded-3xl bg-white p-6 shadow-card"><h2 className="text-2xl font-black">Product moderation</h2><div className="mt-4 space-y-3">{products.slice(0,4).map(p => <div key={p.id} className="flex items-center justify-between rounded-2xl bg-slate-50 p-4"><div><p className="font-bold">{p.title}</p><p className="text-sm text-slate-500">{p.department} · {p.sellerName}</p></div><button className="rounded-full bg-emerald-600 px-4 py-2 text-sm font-bold text-white">Approve</button></div>)}</div></section><section className="rounded-3xl bg-white p-6 shadow-card"><h2 className="text-2xl font-black">Support tickets</h2><div className="mt-4 space-y-3">{tickets.map(t => <div key={t.id} className="rounded-2xl bg-slate-50 p-4"><p className="font-bold">{t.subject}</p><p className="text-sm text-slate-500">{t.status} · {t.priority} · {t.orderId || 'No order'}</p></div>)}</div></section><section className="rounded-3xl bg-white p-6 shadow-card"><h2 className="text-2xl font-black">Audit and configuration</h2><ul className="mt-4 space-y-3 text-sm"><li>✓ RBAC-protected admin routes designed</li><li>✓ Sensitive admin actions logged in schema</li><li>✓ CMS modules, feature flags and campaigns modeled</li><li>✓ Refund and return queues available</li></ul></section></div></div>;
}
