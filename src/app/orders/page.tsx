import { orders, products } from '@/lib/demoData';
import { formatMoney } from '@/lib/commerce';
import { TrustTimeline } from '@/components/account/TrustTimeline';
export default function OrdersPage() { return <div className="container-page py-10"><h1 className="text-4xl font-black">Your orders</h1><div className="mt-6 space-y-5">{orders.map(o => <article key={o.id} className="rounded-3xl bg-white p-6 shadow-card"><div className="flex flex-wrap justify-between gap-3 border-b pb-4"><div><p className="font-black">Order {o.id}</p><p className="text-sm text-slate-500">Placed {new Date(o.createdAt).toLocaleDateString()}</p></div><div className="text-right"><p className="font-black">{formatMoney(o.total)}</p><p className="text-sm text-brand">{o.status}</p></div></div>{o.items.map(i => { const p = products.find(x => x.id === i.productId)!; return <div key={i.productId} className="mt-4 flex justify-between"><span>{p.title} × {i.quantity}</span><span>{o.tracking}</span></div>; })}<TrustTimeline status={o.status} /><div className="mt-5 flex flex-wrap gap-3"><button className="rounded-full bg-brand px-5 py-2 font-bold text-white">Track package</button><a href="/returns" className="rounded-full border px-5 py-2 font-bold">Return or replace</a><button className="rounded-full border px-5 py-2 font-bold">Download invoice</button><a href="/support" className="rounded-full border px-5 py-2 font-bold">Get help</a><button className="rounded-full border px-5 py-2 font-bold">Buy again</button></div></article>)}</div></div>; }
