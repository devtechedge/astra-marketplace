export function SponsoredCampaignCard({ name, budget, spend, status }: { name: string; budget: number; spend: number; status: string }) {
  return <article className="rounded-3xl bg-white p-6 shadow-card"><div className="flex justify-between"><h2 className="text-xl font-black">{name}</h2><span className="rounded-full bg-amber-50 px-3 py-1 text-sm font-bold text-amber-700">{status}</span></div><p className="mt-3 text-sm text-slate-600">Budget ${budget.toFixed(2)} · Spend ${spend.toFixed(2)}</p><div className="mt-4 h-3 rounded-full bg-slate-100"><div className="h-3 rounded-full bg-coral" style={{ width: `${Math.min(100, (spend / budget) * 100)}%` }} /></div><div className="mt-5 flex gap-3"><button className="rounded-full border px-4 py-2 font-bold">Edit</button><button className="rounded-full border px-4 py-2 font-bold">Pause</button></div></article>;
}
