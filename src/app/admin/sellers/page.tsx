import { AdminNav } from '@/components/admin/AdminNav';
import { sellers } from '@/lib/demoData';
import { formatMoney } from '@/lib/commerce';
export default function AdminSellersPage() { return <div className="container-page py-10"><AdminNav /><h1 className="text-4xl font-black">Seller approvals and audits</h1><div className="mt-6 grid gap-4">{sellers.map(s => <article key={s.id} className="rounded-3xl bg-white p-6 shadow-card"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="text-xl font-black">{s.name}</h2><p className="text-slate-600">Rating {s.rating} · Payout {formatMoney(s.payoutBalance)}</p></div><span className="h-fit rounded-full bg-indigo-50 px-4 py-2 font-bold text-indigo-700">{s.status}</span></div><div className="mt-4 flex flex-wrap gap-3"><button className="rounded-full bg-emerald-600 px-5 py-2 font-bold text-white">Approve</button><button className="rounded-full border px-5 py-2 font-bold">Suspend</button><button className="rounded-full border px-5 py-2 font-bold">Audit store</button></div></article>)}</div></div>; }
