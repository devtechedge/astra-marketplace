import { SellerNav } from '@/components/seller/SellerNav';
import { sellerOrders } from '@/lib/expansionData';
import { formatMoney } from '@/lib/commerce';
export default function SellerOrdersPage() { return <div className="container-page py-10"><SellerNav /><h1 className="text-4xl font-black">Seller order queue</h1><p className="mt-2 text-slate-600">Accept, process, ship, cancel or refund seller-fulfilled orders.</p><div className="mt-8 space-y-4">{sellerOrders.map(o => <article key={o.id} className="rounded-3xl bg-white p-6 shadow-card"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="font-black">{o.orderId}</h2><p className="text-slate-600">{o.item} × {o.qty} · {o.buyer}</p></div><div className="text-right"><p className="font-black">{formatMoney(o.value)}</p><p className="text-sm text-coral">{o.sla}</p></div></div><div className="mt-5 flex flex-wrap gap-3"><button className="rounded-full bg-mint px-5 py-2 font-bold text-white">Print packing slip</button><button className="rounded-full border px-5 py-2 font-bold">Add tracking</button><button className="rounded-full border px-5 py-2 font-bold">Message buyer</button><button className="rounded-full border px-5 py-2 font-bold">Refund</button></div></article>)}</div></div>; }
