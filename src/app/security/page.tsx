export default function SecurityPage() { const items = ['Change password', 'MFA setup', 'Active sessions', 'Trusted devices', 'Login alerts', 'Recovery email']; return <div className="container-page py-10"><h1 className="text-4xl font-black">Login and security</h1><p className="mt-2 text-slate-600">Production-style account security controls.</p><div className="mt-8 grid gap-5 md:grid-cols-3">{items.map(i => <div key={i} className="rounded-3xl bg-white p-6 shadow-card"><h2 className="font-black">{i}</h2><p className="mt-2 text-sm text-slate-600">Manage {i.toLowerCase()} with audit logging and session controls.</p></div>)}</div></div>; }
